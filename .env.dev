# =============================================
# Shinhan AAS Server - 개발/테스트 환경변수 설정
# H2 인메모리 DB 사용
# 사용법: docker run --env-file .env.dev -p 6443:6443 shinhan-aas-server:0.0.2
# =============================================

# 서버 포트
SERVER_PORT=6443

# JWT 설정 (개발용)
JWT_SECRET=shinhan-aas-server-secret-key-must-be-at-least-256-bits-long-for-hs256
JWT_ACCESS_TOKEN_VALIDITY=3600000
JWT_REFRESH_TOKEN_VALIDITY=604800000

# H2 Database (기본값 사용 - application.yaml)
# SPRING_PROFILES_ACTIVE 미설정 시 H2 인메모리 사용

# JPA 설정 (개발용)
SPRING_JPA_SHOW_SQL=true
